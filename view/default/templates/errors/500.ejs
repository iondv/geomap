<% layout('../content') -%>

<% block('breadcrumbs', `<li>${__('Ошибка сервера')}</li>`) -%>

<div class="error-page">
    <h2 class="headline text-red"> 500</h2>
    <div class="error-content">
        <h3><i class="fa fa-warning text-red"></i> <%=__('Что-то пошло не так')%></h3>
        <p><%=__('Прямо сейчас мы работаем над устранением ошибки.')%></p>
        <% include _search %>
    </div>
</div>